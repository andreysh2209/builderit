package com.example.builderit.repositories;

import com.example.builderit.models.SubCategoryMaterial;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SubCategoryMaterialRepository extends JpaRepository<SubCategoryMaterial,Long> {
}
